import { createFileRoute, Link } from "@tanstack/react-router";
import { Shell, Panel } from "@/components/tti/Shell";
import { holdings, inr, portfolioStats } from "@/lib/market-data";

export const Route = createFileRoute("/portfolio")({ component: Portfolio });

function Portfolio() {
  const stats = portfolioStats();
  return <Shell><div className="mx-auto max-w-6xl space-y-4"><div className="flex flex-wrap items-end justify-between gap-3"><div><h1 className="font-display text-2xl font-bold">Portfolio</h1><p className="mt-1 font-mono text-[10px] uppercase tracking-wider text-muted-foreground">Holdings · Performance · Paper portfolio</p></div><Link to="/markets" className="rounded-lg border border-line px-4 py-2 font-mono text-[10px] uppercase tracking-wider hover:bg-white/[0.05]">Open Markets →</Link></div><div className="grid grid-cols-2 gap-3 lg:grid-cols-4">{[["Invested",stats.invested],["Current Value",stats.current],["Total P&amp;L",stats.pnl],["Day P&amp;L",stats.dayPnl]].map(([label,value])=><div key={String(label)} className="rounded-xl border border-line bg-white/[0.03] p-4"><div className="font-mono text-[10px] uppercase text-muted-foreground">{String(label).replace("&amp;","&")}</div><div className="mt-2 font-mono text-lg">{inr(Number(value))}</div></div>)}</div><Panel title="Holdings" tag="PAPER PORTFOLIO" meta={`${holdings.length} POSITIONS`}><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left"><thead className="border-b border-line font-mono text-[9px] uppercase text-muted-foreground"><tr><th className="px-4 py-3">Stock</th><th className="px-4 py-3">Qty</th><th className="px-4 py-3">Avg</th><th className="px-4 py-3">LTP</th><th className="px-4 py-3">Value</th><th className="px-4 py-3">P&amp;L</th><th className="px-4 py-3">Today</th></tr></thead><tbody className="divide-y divide-line">{holdings.map(h=>{const pnl=(h.ltp-h.avg)*h.qty;return <tr key={h.symbol} className="hover:bg-white/[0.03]"><td className="px-4 py-3"><div className="font-mono text-[11px] font-semibold">{h.symbol}</div><div className="text-[10px] text-muted-foreground">{h.name} · {h.sector}</div></td><td className="px-4 py-3 font-mono text-[11px]">{h.qty}</td><td className="px-4 py-3 font-mono text-[11px]">{inr(h.avg)}</td><td className="px-4 py-3 font-mono text-[11px]">{inr(h.ltp)}</td><td className="px-4 py-3 font-mono text-[11px]">{inr(h.ltp*h.qty)}</td><td className={`px-4 py-3 font-mono text-[11px] ${pnl>=0?"text-up":"text-down"}`}>{pnl>=0?"+":""}{inr(pnl)}</td><td className={`px-4 py-3 font-mono text-[11px] ${h.changePct>=0?"text-up":"text-down"}`}>{h.changePct>=0?"+":""}{h.changePct.toFixed(2)}%</td></tr>})}</tbody></table></div></Panel><div className="grid gap-4 md:grid-cols-2"><Panel title="Portfolio Return" tag="PERFORMANCE"><div className="p-4"><div className={`font-mono text-3xl ${stats.pnl>=0?"text-up":"text-down"}`}>{stats.pnl>=0?"+":""}{inr(stats.pnl)}</div><div className={`mt-2 font-mono text-sm ${stats.pnl>=0?"text-up":"text-down"}`}>{stats.pnlPct>=0?"+":""}{stats.pnlPct.toFixed(2)}% overall · {stats.dayPct>=0?"+":""}{stats.dayPct.toFixed(2)}% today</div></div></Panel><Panel title="Portfolio Access" tag="CUSTOMER VIEW"><div className="p-4 text-sm text-muted-foreground">Customers can open this page from the Portfolio menu and review holdings, current value and paper-trading performance.</div></Panel></div></div></Shell>;
}
